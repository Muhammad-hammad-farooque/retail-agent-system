# Inherits root .gitignore — this file adds retail-agent-system-specific rules

# One-time migration/fix scripts (contain DB logic, not for production repo)
migrate_*.py
fix_*.py

# Frontend build output
frontend/.next/
frontend/node_modules/
frontend/out/
